syntax = "proto3";

package cml.device;

import "common/config.proto";

message Action {
    cml.common.Config common_config = 1;
    string user_uuid = 2;
    string trigger_topic = 3;
    uint32 trigger_state = 4;
    uint32 response = 5;
}
